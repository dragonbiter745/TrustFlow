{
  "name": "trustflow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "setup": "npm run install:all && echo 'Setup complete. Copy .env.example to .env in both /backend and /frontend and fill in values.'"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
